{
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.0.11",
    "bcrypt": "^5.1.1"
  },
  "scripts": {
    "test": "",
    "postinstall": "npx husky init",
    "prepare": "husky"
  },
  "husky": {
    "hooks": {
      "commit-msg": "npx --no-install commitlint --edit $1",
      "post-merge": "npx husky install"
    }
  }
}
